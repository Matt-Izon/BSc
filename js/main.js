$(document).ready(function(){		$('.main-gallery').flickity({		// options		cellAlign: 'center',		wrapAround: true,		prevNextButtons: false,		autoPlay: 2000,		dragThreshold: 1,		selectedAttraction: 0.08, // How fast the slider moves		friction: 0.6, // 		draggable: '>1'	});		$('.main-gallery2').flickity({		// options		cellAlign: 'center',		watchCSS: true,		wrapAround: true,		prevNextButtons: false,		autoPlay: 2000,		dragThreshold: 1,		selectedAttraction: 0.04, // How fas the slider moves		friction: 0.6 // 	});		$('.event-gallery').flickity({		// options		cellAlign: 'center',		wrapAround: true,		prevNextButtons: false,		autoPlay: 2000,		dragThreshold: 1,		selectedAttraction: 0.08, // How fast the slider moves		friction: 0.6, // 		draggable: '>1'	});		// Shows the active page that the user is on	$(function(){		$('.deskNav a').each(function() {			if ($(this).prop('href') == window.location.href.split('#')[0]) {				$(this).addClass('page-active');			}		});	});		var $hamburger = $(".hamburger");	var $mobileNav = $(".mobNav");		$hamburger.click(function() {		$hamburger.toggleClass("is-active");		// Do something else, like open/close menu		$mobileNav.toggleClass("is-active");		$(".main-container").toggleClass("is-open");		$("#logoContainer").toggleClass("is-open");		$("#logoContainer1").toggleClass("is-open");		$("body").css("overflow", "hidden");				$(document).click(function(event) {			//if you click on anything except the menu itself or the "open modal" link, close the modal			if (!$(event.target).closest(".mobNav,.is-active").length) {				$("body").find($mobileNav).removeClass("is-active");				$("body").find($hamburger).removeClass("is-active");				$(".main-container").removeClass("is-open");				$("#logoContainer").removeClass("is-open");				$("#logoContainer1").removeClass("is-open");				$("body").css("overflow", "scroll");			}		});			});		var logo = $('#logo'),  w = logo.width();	    $(window).scroll(function () {        if ($(this).scrollTop() > 30) {			$('#logoContainer').css({				paddingTop: '0px'			});            logo.css({				width: w/2.3,				marginLeft: '72%'			});        } else{			$('#logoContainer').css({				paddingTop: '60px'			});            logo.css({				width: w,				marginLeft: '0%'			});        }    });		$(window).scroll(function () {		if($(this).scrollTop() > 300) {			$('#arrowBlock').css({				right: '0',				transition: 'all ease-in-out .2s',				animationPlayState: 'running'			});		}		else {			$('#arrowBlock').css({				right: '-100px',				transition: 'all ease-in-out .2s',				animationPlayState: 'paused'			});		}	});			$('.accordion').click(function() {		$(this).toggleClass('active1');				$(this).next('.panel').slideToggle();	});		$('.show-more').click(function() {				$(this).prev('.bioText').slideToggle();        $(this).next('.playerSponsor').slideToggle();				$(this).text(function(i, v){			return v === 'Hide Bio' ? 'Read Bio' : 'Hide Bio'		})	});	})